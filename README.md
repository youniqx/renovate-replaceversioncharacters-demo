# Renovate replace-characters-in-version demo (currently not working)

This repo is here to test whether renovate can replace certain strings
in the upstream version with others (in this case `.` with `_`).
